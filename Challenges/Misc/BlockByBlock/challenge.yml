name: "Blockbyblock"
author: "SPYN0ZA"
category: blockchain
description: |-
  Sometimes every block contains something weird .

   NOTE!:
   you only need to spawn an instance, no need to press the "Flag" button.
   the isSolved() function will always return false afterall.

   Connect: http://{{ .host }}:40012

    **author**: SPYN0ZA
value: 500
type: dynamic
extra:
  initial: 500
  decay: 20
  minimum: 10
flags:
  - CyberSphere{every_mined_block_has_some_secrets_innit}
files:
  - handout/
state: hidden
version: "0.1"
